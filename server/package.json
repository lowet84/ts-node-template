{
  "name": "ts-node",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "dev": "ts-node-dev --inspect --respawn --transpileOnly server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node server.ts",
    "build": "browserify server.ts -p [ tsify --noImplicitAny ] > ../dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.11.1",
    "browserify": "^16.2.2",
    "gulp": "^3.9.1",
    "ts-loader": "^2.0.1",
    "ts-node-dev": "^1.0.0-pre.23",
    "tsify": "^4.0.0",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "express": "^4.16.3"
  }
}
